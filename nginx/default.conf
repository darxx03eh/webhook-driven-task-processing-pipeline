server {
    listen 80;
    server_name darawsheh.dev www.darawsheh.dev api.darawsheh.dev;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name darawsheh.dev www.darawsheh.dev;

    ssl_certificate /etc/letsencrypt/live/darawsheh.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/darawsheh.dev/privkey.pem;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        return 308 https://api.darawsheh.dev$request_uri;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 443 ssl;
    server_name api.darawsheh.dev;

    ssl_certificate /etc/letsencrypt/live/darawsheh.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/darawsheh.dev/privkey.pem;

    location / {
        proxy_pass http://backend:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api/ {
        proxy_pass http://backend:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
